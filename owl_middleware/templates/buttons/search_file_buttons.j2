[
    {% for file_name in file_names %}
    {% set safe_name = file_name|truncate(30) %}
    {% set callback_data = "file_" ~ safe_name|replace(" ", "_")|replace("-", "_")|replace(".", "_")|lower %}
    {"text": "📦 {{ file_name }}", "callback_data": "{{ callback_data }}"}
    {% if not loop.last %},{% endif %}
    {% endfor %}
    {% if files_count > 0 %},
    {"text": "🔄 Обновить список", "callback_data": "refresh_files"},
    {"text": "⬅️ Назад", "callback_data": "main_menu"}
    {% endif %}
]