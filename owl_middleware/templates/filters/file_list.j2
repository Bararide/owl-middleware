{% block content %}
{% if files and files | length > 0 %}
📁 <b>Ваши файлы</b>

{% for file in files %}
📄 <b>{{ file.name }}</b>
├ ID: <code>{{ file.id }}</code>
├ Размер: {{ file.size | filesizeformat if file.size else "Неизвестно" }}
├ Контейнер: <code>{{ file.container_id }}</code>
├ Создан: {{ file.created_at.strftime('%d.%m.%Y %H:%M') if file.created_at else "Неизвестно" }}
└ Тип: {{ file.mime_type or "Неизвестно" }}

{% endfor %}

📊 Всего файлов: {{ files | length }}
{% else %}
📭 <b>Файлы не найдены</b>

У вас пока нет загруженных файлов.
Используйте команду /upload для загрузки файлов.
{% endif %}
{% endblock %}